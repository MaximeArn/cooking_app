import 'package:flutter/material.dart';

class GroupsRankingLine extends StatelessWidget {
  const GroupsRankingLine({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}